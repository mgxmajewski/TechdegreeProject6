extends layout

block content

    h1 Page not found ;(
    h2= error.status
    h3= error.message
    pre= error.stack

    nav.nav
        a(href='/')
            span &larr;
            |  Back
